# Hi, I'm Vishal 👋

**Product Leader & Builder — Agentic AI, AI/ML-Powered Customer Experiences & Platform Products**

AI-native product leader with 10+ years defining strategy, vision, roadmap, and launching 0-to-1 products and 1-to-100 platforms across fintech, wealth management, telecom, retail, ecommerce, and healthcare — spanning B2B, B2C, and B2B2C.

## 💼 Currently
Leading product strategy for **Digital Advisor** and **Personal Advisor** at **Vanguard** ($6B+ LOB, 4M+ MAU) — architecting the next-generation Agentic AI Digital Advisor from 0-to-1, including model evaluation frameworks (correctness, groundedness, safety, latency) and FINRA/SEC-compliant responsible AI design.

## 🚀 Selected work
- **Vanguard** — Agentic AI Digital Advisor (0→1): LLM orchestration, autonomous agent workflows, personalized financial guidance at scale, with regulatory guardrails and model governance.
- **T-Mobile** — Launched one of the first autonomous enterprise Agentic AI platforms in US telecom: **75% adoption, 46% automation, 60% containment, 80% CSAT, 30% fewer support calls**. Built the IntentCX AI governance & model evaluation framework (aligned to NIST AI RMF), adopted org-wide by 3 additional teams.
- **eBay** — Drove **$300M+ in savings** via marketplace platform modernization and API standardization across hundreds of engineering teams.

## 🎓 Background
Stanford University Graduate School of Business · Google AI certifications (AI Essentials, Responsible AI, Prompting, AI Tools for Productivity)

## 🔗 Why this portfolio exists
The "model evaluation frameworks" line above isn't abstract — it's the same discipline, one layer down. At Vanguard and T-Mobile that means correctness/groundedness/safety/latency checks on an agent's outputs; below is the same four-axis taxonomy applied to the MCP tools an agent actually calls: is the tool documented well enough to use correctly (`mcp-doctor`), does it fail safely on bad input and respond fast enough to matter (`mcp-fuzz`), does its response actually reflect reality (`mcp-reality-check`). Same eval mindset, different substrate — built and dogfooded against 40+ real, in-the-wild MCP servers up to 61k★, not synthetic fixtures.

**What this doesn't cover, by design**: semantic hallucination judgment ("is this answer actually true" needs an LLM judge, which breaks the fully-deterministic, no-API-cost property all three tools share) and dynamic agent-side red-teaming (`mcp-doctor`'s security score audits a tool's own code and description for injection risk — tool poisoning, dangerous exec — not whether a live agent can be manipulated by a malicious tool response at runtime). **Runtime authorization enforcement** (does a tool actually refuse an out-of-scope action, not just claim to) is a related, permanent no for a different reason: everything else here works because it's a generic pattern meaning the same thing across any server; "out of scope" is different for every server (a project ID, a directory, a schema), and testing it for real needs per-target configuration — that's manual security review, not something a generic auditor can do honestly. Two more, handled differently: **observability/structured-logging completeness** was evaluated and explicitly declined — there's no spec-mandated logging format to check against, and the only proxy I could think of (flagging a tool with a `Context` param that never logs anything) would misfire on any simple tool that legitimately has nothing to log; not worth shipping a weak heuristic just to claim coverage. **Drift/regression tracking** and **supply-chain (unpinned dependency) checking** — both closed since: the leaderboard diffs every re-scan against whatever was live before it, and `mcp-doctor` now flags a dependency with no version floor at all. Real, named gaps, not swept under "the trilogy covers everything."

## 🧱 Portfolio
- 📄 [`ai-pm-portfolio`](https://github.com/vishalhabib99/ai-pm-portfolio) — PRDs, working prototypes, and honestly-reported evals (including failures, not just wins)
- 🩺 [`mcp-doctor`](https://github.com/vishalhabib99/mcp-doctor) — static-analysis CLI that audits MCP server implementations for the things that break an agent calling them (`pip install mcp-server-lint`, or a [GitHub Marketplace Action](https://github.com/marketplace/actions/mcp-doctor)). Dogfooded against 40+ real MCP servers in the wild across Python, TypeScript, and Go — up to 50k★, including official servers from GitHub, HashiCorp, Red Hat, Brave, and MathWorks — found and fixed 41 genuine bugs, verified against each repo before/after, including two real security-heuristic false positives (a plain `RegExp.exec()` call and Playwright's `$eval()` DOM helper both misflagged as dangerous code execution) caught auditing `arabold/docs-mcp-server`, and a false "no description" on two real tools caught auditing `oraios/serena` (29K★) — the tool's description was being read from the wrong docstring, verified directly against serena's own registration code. One fix led to a [merged PR](https://github.com/homeassistant-ai/ha-mcp/pull/2327) into `ha-mcp` (4.5K★, active); a separate clean pass surfaced a real doc-coverage gap [filed upstream](https://github.com/haris-musa/excel-mcp-server/issues/158) in `excel-mcp-server` (4.1K★, active). Added a **read-only annotation-mismatch check** — flags a tool declaring `readOnlyHint: true` whose own code contains a real write signature (raw SQL mutation, a file opened for writing, a mutating HTTP call) — a real, recurring bug class, not hypothetical: `mcp-fuzz`'s own `codebase-memory-mcp#2118` finding below found 13 of 15 tools mislabeled this exact way. Most recently, added `--diff-against`, a schema/contract diff mode: saves a run's tool signatures as a baseline and catches a breaking change later — a tool that's gone, a parameter that's gone, or one that's newly required — the exact gap a real commenter on the trilogy LinkedIn article (Tyler Robinson) named directly ("contract consistency"). Verified against real git history, not just synthetic fixtures: diffed `jingcheng-chen/rhinomcp` 20 commits behind its own `main`, honestly clean (tools added, none removed). Live results: a [public leaderboard](https://vishalhabib99.github.io/mcp-doctor/) ranking every audited repo by quality + security grade, each with a real per-repo badge maintainers can embed in their own README — now with **drift tracking**, comparing every re-scan against whatever was live before it so a regression shows a real ▲/▼ delta instead of silently overwriting the old score. Caught a real cross-environment gotcha while verifying it (a Python 3.11-vs-3.12 parsing difference reading as a false "regression" locally, confirmed harmless since production scans are always 3.12-vs-3.12) — traced to root cause and documented rather than shipped blind. Then ran the full toolkit — every check across all three tools — against one fresh, real target at once for the first time: `sooperset/mcp-atlassian` (5.9K★, real Jira/Confluence integration), 98 tools, 100%/A quality, 99%/A security, `--diff-against` a commit 30 revisions back honestly clean. Also added an **unpinned-dependency (supply-chain) check** — flags a `requirements.txt`/`package.json` dependency with no version floor at all, excluding `>=`/caret/tilde ranges (standard practice, not a real signal on their own) to keep it low-noise — verified clean against six real repos. Then, dogfooding `sooperset/mcp-atlassian` and `jingcheng-chen/rhinomcp` found the tool-name-uniqueness check had no concept of *which server* a tool belongs to — it flagged real, non-duplicate tools as spec violations whenever a repo composed multiple FastMCP servers (`.mount(x, namespace=...)`) or shipped independent standalone entrypoints alongside the real one, a monorepo pattern. Fixed both: resolved `.mount()` calls to apply the real namespaced tool name before the dedup check, and scoped the check to each standalone `if __name__ == "__main__"` entrypoint separately — 11 of 12 false duplicates across the two repos cleared, the one remaining a genuine cross-file FastMCP-instance alias correctly left flagged rather than guessed at. Then, fixed the first bug an external contributor found rather than one this project's own dogfooding surfaced: [Edward Izgorodin](https://github.com/modelcontextprotocol/modelcontextprotocol/discussions/3322) reported that `--diff-against` falsely flagged a shared, unchanged, required parameter as removed whenever the baseline and current run used two different Python tool-registration styles (a FastMCP-decorated function vs. the raw `Tool(inputSchema=...)` constructor) — the raw-schema style never extracted parameter names to compare in the first place, so "coverage not attempted" silently read as "no parameters." Fixed at the source, with his exact repro kept as a permanent regression test, shipped as v0.9.3. Most recently, a second real report from the same discussion thread: [Christian Bru](https://github.com/modelcontextprotocol/modelcontextprotocol/discussions/3322) pointed out that the spec's own doc comment on `destructiveHint` says it's meaningful only when `readOnlyHint == false`, but nothing in the JSON Schema enforces that — so a tool can declare `readOnlyHint: true` and an explicit `destructiveHint` at the same time, a combination the spec defines as having no meaning at all, and a realistic copy-paste mistake when a new tool is cloned from an existing write-tool's annotation block. Added a new **annotation-contradiction check** flagging exactly that pair regardless of `destructiveHint`'s value, shipped as v0.9.4. Most recently, turned the description-quality and annotation-contradiction checks into a live registration gate (`check_tool_registration`, v0.10.0) — checks a tool's live `tools/list` metadata instead of parsing source, dependency-free and connection-free (mcp-doctor has never made a live call to anything; this doesn't change that). Dogfooding it against the real official `server-memory` reference server before shipping found the direct port of the static rule (flag `destructiveHint` merely being *present*) false-positived on every tool — that server explicitly sets all four annotation fields as good practice, not a bug. Fixed to require `destructiveHint` actually be `true`, not just present; re-verified 0/9 false positives afterward. Completes the trilogy's live-agent surface: `mcp-doctor` now gates registration, `mcp-fuzz` gates latency, `mcp-reality-check` gates correctness — one live check per original tool. Most recently, added a **URL-format-hint check**: a `url`-shaped string parameter (`url`, `icon_url`, `target_url`, etc.) with no `format: "uri"` JSON-schema hint gives a schema-only client no signal it needs to be a real URL, not just any string — found for real by dogfooding [mcp-fuzz](https://github.com/vishalhabib99/mcp-fuzz) against `jon-the-dev/linkding-mcp-server` ([issue #73](https://github.com/jon-the-dev/linkding-mcp-server/issues/73)), then confirmed to recur independently across `sooperset/mcp-atlassian`, `homeassistant-ai/ha-mcp`, `MODSetter/SurfSense`, `0x4m4/hexstrike-ai`, and `CursorTouch/Windows-MCP` before shipping it as a check. Released as `v1.11.0` / PyPI `0.11.0`. Most recently, fixed a second real report from the same [Edward Izgorodin discussion thread](https://github.com/modelcontextprotocol/modelcontextprotocol/discussions/3322): after verifying the earlier `--diff-against` fix shipped, he found that a raw-schema property whose key isn't a string literal (a variable used as a dict key) still couldn't be attributed a name, so a genuinely-present parameter was reported as removed when diffed against a baseline that did resolve it. Added a `param_names_complete` coverage flag, consulted before trusting an absence as a real removal — released as `v1.9.5` / PyPI `0.11.1`. Most recently, closed an already-documented, self-found limitation rather than another external report: the description-length heuristic (`<10 chars → likely restates the name`) misfired on complete, well-formed CJK descriptions (e.g. 9 Chinese characters conveying a full sentence, on `xpzouying/xiaohongshu-mcp`), since it was calibrated for English character density. Fixed with a `description_display_width` helper — the same convention terminals and `wcwidth` use, weighting a Wide/Fullwidth character as 2 display columns — applied across the Python, Go, and TS analyzers and the live registration gate. Released as `v1.9.6` / PyPI `0.11.2`. Most recently, fixed a real false positive found dogfooding a fresh target, [`blazickjp/arxiv-mcp-server`](https://github.com/blazickjp/arxiv-mcp-server) (3.1k★, not previously audited): the dangerous-exec security check flagged `scripts/smoke_installed_wheel.py`, a release smoke-test that installs a built wheel into a throwaway venv via fixed, non-tool-derived `subprocess.run()` calls, as if it were reachable tool code. Broadened the existing test-file exclusion to also recognize a `scripts/` path component — breadth-checked first (`aws/chalice`, `colobot/colobot`, `diem/diem` all follow the same convention) rather than treating one repo as enough evidence. 90%→97%/A security on that repo. Also caught and fixed a real versioning bug while releasing it: the Action's `v1.x` tags had gone non-monotonic since `v1.11.0` (2026-09-18) — three later fixes shipped as `v1.9.5`/`v1.9.6`/`v1.9.7` instead of continuing past it, a numbering mistake, not a branch — confirmed via `git merge-base` that `v1.11.0`'s commit is an ancestor of all three. Deleted the mis-numbered tag/release and re-cut as `v1.11.3`, continuing the correct line. Released as `v1.11.3` / PyPI `0.11.3`. Most recently, fixed a real detection gap found dogfooding a fresh target, [`zcaceres/markdownify-mcp`](https://github.com/zcaceres/markdownify-mcp) (3k★): tools are collected via `import * as tools from "./tools.js"` and returned as `{ tools: Object.values(tools) }` from the low-level SDK's list-tools handler, each wrapped in Zod's `Schema.parse({...})` — neither the namespace-import `Object.values()` shape nor the `.parse()` wrapper were recognized, reporting 0 of 11 tools. Confirmed not a one-off first (`flesler/mcp-tasks` uses the identical `Object.values(tools)` idiom, though via a runtime loop — a separate, still out-of-scope pattern, same class as the already-documented `mcp-server-browserbase` `.forEach()` case) before adding namespace-import resolution and a generic `.parse(...)` unwrap. 0→11 tools, 100%/A quality, 98%/A security. Released as `v1.11.4` / PyPI `0.11.4`. Most recently, fixed a second real detection gap found dogfooding a fresh target, [`MinishLab/semble`](https://github.com/MinishLab/semble) (6.1k★): 21 of 22 dangerous-exec flags were false positives, all in `benchmarks/`/`benchmarks/baselines/` — a performance-comparison harness shelling out to competing CLI tools, not reachable from either of the repo's 2 real MCP tools. Breadth-checked first (`pypa/pipenv`, `pytorch/xla`, `facebookexperimental/hermit` all follow the same convention) before broadening the existing `scripts/` exclusion to also cover `benchmarks/`. 62%/D→88%/B security; the one remaining flag (a `git clone` of a caller-supplied URL in the real indexing code) is genuine and correctly still caught. Released as `v1.11.5` / PyPI `0.11.5`. Most recently, fixed a real false positive found dogfooding a fresh target, [`getsentry/XcodeBuildMCP`](https://github.com/getsentry/XcodeBuildMCP) (6.4k★): a local `const exec = executor ?? getDefaultCommandExecutor();` (a dependency-injected command runner, for testability) was misflagged as `child_process.exec` by the bare-identifier dangerous-exec regex, which can't tell a locally rebound name from the real primitive. Confirmed not a one-off first (the same `const exec = executor ?? ...` idiom recurs independently in `Seitrace/seitrace-mcp`, another real MCP server) before adding a scope-aware shadow check — only the two bare-identifier patterns it can actually rebind are suppressed, the qualified ones (`os.system`, `child_process.exec`, `exec.Command`) are untouched. 45%/F→70%/C security on that repo. Released as `v1.11.6` / PyPI `0.11.6`. Most recently, fixed a real README-coverage false positive found dogfooding [`firecrawl/firecrawl-mcp-server`](https://github.com/firecrawl/firecrawl-mcp-server) (7.5k★): two live, callable tools whose own description opens "Deprecated compatibility entry point..." were flagged as undocumented gaps, when their absence from the README was deliberate — they're compatibility shims that always error/redirect to a replacement. Fixed by skipping a tool from the undocumented-tool check when its own description says it's deprecated, then independently reconfirmed against a second, unrelated codebase, [`exa-labs/exa-mcp-server`](https://github.com/exa-labs/exa-mcp-server) (5k★): 6 genuinely deprecated tools, three different notice styles (`[Deprecated: Use X instead]`, `⚠️ DEPRECATED:`, `[Deprecated]`), all correctly suppressed — leaving exactly one real, still-undocumented tool (`get_code_context_exa`) correctly flagged. Released as `v1.11.7` / PyPI `0.11.7`.
- 🧪 [`mcp-fuzz`](https://github.com/vishalhabib99/mcp-fuzz) — mcp-doctor's companion (`pip install mcp-runtime-check`): instead of reading source, it launches a real MCP server and calls its tools with inputs derived from their own JSON schema, checking whether the server fails cleanly (a structured error) or crashes/hangs on missing or wrong-typed input. 22 real-world passes so far, up to 61K★ (`upstash/context7`) — including Ant Design's official `mcp-server-chart` (4.3K★) — all 27 tools crashed instead of erroring cleanly on realistic bad input, [filed upstream](https://github.com/antvis/mcp-server-chart/issues/323) and confirmed fixed on `main` after a live rescan — a real crash bug [filed on `shadcn-ui-mcp-server`](https://github.com/Jpisnice/shadcn-ui-mcp-server/issues/61) (3K★), and a tool-annotation mislabeling [filed on `codebase-memory-mcp`](https://github.com/DeusData/codebase-memory-mcp/issues/2118) (42.7K★, official-scale). An external maintainer ([`agent-inspect`](https://github.com/rajudandigam/agent-inspect), a trajectory-debugging tool for TypeScript agents) asked to feed a real mcp-fuzz session into their evidence model — shipped `--full-trace` (v0.1.5) for full-fidelity JSONL export in response. The finding it surfaced (a healthy, 100%/A crash-resilience session reads as almost entirely failed once isError:true rejections collapse into a generic error status) was substantial enough that the maintainer asked for a reproducible write-up — filed as [agent-inspect#362](https://github.com/rajudandigam/agent-inspect/issues/362), with a verified fix demonstration built against AgentInspect's own outcome_observed schema. AgentInspect shipped a fix in response (v6.26.0, `--preset behavioral-session`) that scores outcome-level failures without collapsing a healthy session into a failed run, verified against the same sanitized gist and merged into their own repo's recipe fixtures. Also hardened the schema generator against adversarial `tools/list` metadata — found and fixed three real self-bugs (non-dict schema fields, a 5000-level-deep schema blowing the recursion limit). Also ran end-to-end against `oraios/serena` (29K★) with a real Python language server actually active (not a stub) — 28/28 tools tested with `--include-destructive`, 0 crashes, 100%/A, the deepest real-server coverage of any repo audited so far. Also added a real, scored **latency check** — the fourth axis (correctness/safety/groundedness/latency) alongside the other two tools — flagging a tool's valid call as slow via an absolute threshold or as a >3x outlier against its own server's median; re-running against `oraios/serena` found two real relative outliers (`create_text_file` at 9.4x median, `get_current_config` at 4.6x), 92.9%/B. Most recently, added a matching **response-size check** — same design, applied to payload size instead of response time, catching a tool that burns an agent's context window for no reason its description would predict; re-running against `oraios/serena` found four real relative outliers (`onboarding` at 17.2x median, `get_current_config` at 13.4x, plus two smaller ones), 85.7%/C. Most recently, added an opt-in **concurrency check** (`--concurrency N`): launches N independent connections (each its own subprocess) and calls a tool with identical arguments on all of them at once — a real test of concurrent access to a shared backend (a file, database, lock), not synthetic fan-out over one connection. Verified two ways: a new fixture tool with a genuine, reproducible race (a naive create-exclusive lock file) gets caught reliably; the official `modelcontextprotocol/server-memory` reference server comes back clean — an honest result, since each of its subprocesses keeps its own independent state, nothing to race on. Same full-checklist pass on `sooperset/mcp-atlassian` (5.9K★): 58/98 tools tested, 100%/A across crash resilience, latency, response size, and concurrency — the largest real target run against the complete toolkit, genuinely clean. Most recently, added an opt-in **resource-lifecycle check** (`--sequential`): every check above tests one tool call in isolation with synthetic arguments — this one detects `create_X`/`get_X`/`delete_X`-shaped tool groups by name, chains the *real* id a create call actually returns into the read/delete calls, then re-reads that id after deletion to catch a stale read (a resource that still reads as present after being deleted). Verified against a deliberately-planted bug in the test fixture (a fake delete that reports success but never removes the record) — caught correctly, with a clean healthy trio alongside it producing no false positive. Shipped as v0.5.0. Most recently, extended `--sequential` to a different bug class: **cross-resource (parent/child) lifecycle checking** — detects when one resource's create call takes another resource's real id as a parameter, then runs create-parent → create-child → delete-parent → read-child, flagging a still-readable child as a neutral orphaned-reference observation rather than a scored pass/fail (cascade-delete and orphan-allowed are both legitimate designs). Verified against two planted fixture cases: a non-cascading pair (caught) and a correctly-cascading pair (correctly not flagged). Shipped as v0.6.0. Most recently, turned the latency/response-size checks into a live runtime gate (`LatencyGate`, v0.7.0) — same absolute-threshold-plus-outlier design, applied to real calls an agent makes during a live session instead of one synthetic call per tool. One real design change, not a mechanical port: a batch audit only ever compares one tool's single call against unrelated *sibling* tools on the same server, but a live session calls the same tool repeatedly, so the gate tracks each tool's own call history and flags what's unusual for *that tool*, not its neighbors. Verified against a real crash and a real timeout (new fixture tools, not mocked) and dogfooded live against the official `@modelcontextprotocol/server-memory` reference server. Pairs with `mcp-reality-check`'s new correctness gate below — one call site, two independent live checks. Most recently, added a **token cost estimate**: rolls the response-size check's existing ~4-chars/token rule of thumb up into one whole-session total — what it would cost an agent's context window to call every tested tool once. Deliberately tokens only, never dollars — a real `$` figure depends on which model consumes the response and its current per-token pricing, neither of which this tool can know without guessing. Always computed, no new flag, since it's a pure rollup of data the response-size check already gathers. Released as `v0.8.0`.
- 🩻 [`mcp-reality-check`](https://github.com/vishalhabib99/mcp-reality-check) — completes the trilogy (`pip install mcp-reality-check`): mcp-doctor asks *is it documented*, mcp-fuzz asks *does it fail safely*, this asks *does it actually work*. Calls each tool once with a realistic (not placeholder) input and checks the response for a refusal disguised as success ("I don't have access to..." wrapped in `isError: false`), empty content on a claimed success, and violations of a tool's own declared output schema — all fully deterministic, no LLM judge, no API key, no per-call cost. 18+ real-world passes so far, most recently corroborating a clean `mcp-fuzz` pass on `arabold/docs-mcp-server` — found and fixed a real bug in its own input generator (a missing "timezone" hint broke every call to `mcp-server-time`), then hardened the same generator against the malformed-metadata bug class found in mcp-fuzz. Everything else clean. Also corroborated the same `oraios/serena` LSP-backed run as mcp-fuzz above: 10/10 checkable tools, 0 flagged, 100%/A. Most recently, found a real bug in itself while dogfooding `sooperset/mcp-atlassian`: no `--env` flag existed at all, so any config-gated server (this one registers zero tools without Jira/Confluence credentials) was silently untestable — `mcp-fuzz` had already solved the identical problem months earlier for `brave-search-mcp-server`. Added `--env`, re-verified: 58 tested/40 skipped, matching `mcp-fuzz`'s count exactly, 100%/A. Most recently, turned the same checks into a live runtime gate (`guarded_call`, v0.3.0) — instead of only a one-shot batch audit, an agent can now run every real tool call it makes through the same refusal/empty-content/schema-violation checks at call time, catching a lying-but-successful response before it reaches the agent's context. Deliberately scoped to correctness, not security — the runtime-security-proxy space (secrets, prompt injection, destructive-command policy) already has real, actively-maintained tooling elsewhere; this answers a different question that space doesn't: a call can be perfectly safe and still misrepresent what it did. Verified against a real crash and a real timeout (two new fixture tools, not mocked) and dogfooded live against the official `@modelcontextprotocol/server-memory` reference server — a real `create_entities` call and a real `read_graph` call, both correctly passing unflagged.
- 🛡️ [`mcp-trust-check`](https://github.com/vishalhabib99/mcp-trust-check) — the three tools above as one [GitHub Marketplace Action](https://github.com/marketplace/actions/mcp-trust-check): installs and runs all three against a target server and posts one combined score/PR comment instead of three separate ones. Static-only mode (mcp-doctor) needs no config beyond a checkout; the full trilogy also needs a `run` input with the command that launches the server. Self-verified live against the official `@modelcontextprotocol/server-memory` reference server before shipping: doctor 86%/B, fuzz 100%/A, reality-check 100%/A, combined 95.33%/A. 🎥 [35s live demo](https://github.com/vishalhabib99/mcp-trust-check#demo). `v1.0.1`. Most recently, became a Python package too (`pip install mcp-trust-check`, v0.1.0) — `GuardedSession`, the same combined idea applied to a real agent session instead of a CI run: wraps a live `ClientSession` and runs all three tools' now-completed live gates (mcp-doctor's registration checks, mcp-fuzz's latency gate, mcp-reality-check's correctness gate) at their natural point. Deliberately not a naive composition of the three existing call-and-check wrappers, each of which makes its own real call — that would mean two real invocations per logical call, wrong for a non-idempotent or destructive tool. `GuardedSession` calls a tool exactly once and fans the single real response out to each sibling package's own pure per-response functions instead, verified directly with a real call-counting test. Dogfooded live against the official `server-memory` reference server, and confirmed with a completely fresh `pip install` in a clean environment, not just the dev copy.

## ✍️ Writing
- [I Built Three Tools to Audit MCP Servers for Agentic AI. Here's What They Found — and What I Learned Shipping Them.](https://www.linkedin.com/pulse/i-built-three-tools-audit-mcp-servers-agentic-ai-heres-vishal-habib-kmv3c/) — the full trilogy story on LinkedIn, including why I built it (grounded in my Agentic AI model-evaluation work at Vanguard) and what each tool found
- [Building T-Mobile's First Enterprise Agentic AI Platform: 25M Users, 75% Adoption, and What I'd Do Differently](https://www.linkedin.com/pulse/building-t-mobiles-first-enterprise-agentic-ai-platform-vishal-habib-bftoc/) — the platform story on LinkedIn, including the evals/governance framework this portfolio's tools directly extend
- [I built three tools to audit MCP servers. Each one found a bug in itself first.](https://dev.to/vishalhabib99/i-built-three-tools-to-audit-mcp-servers-each-one-found-a-bug-in-itself-first-5dlc) — the trilogy's origin story, on dev.to

## 📫 Reach me
- Email: [vishalhabib99@gmail.com](mailto:vishalhabib99@gmail.com)
- LinkedIn: [in/vishal-habib](https://www.linkedin.com/in/vishal-habib/)
- Website: [vishalhabib.netlify.app](https://vishalhabib.netlify.app/)
- dev.to: [@vishalhabib99](https://dev.to/vishalhabib99)
- GitHub: [@vishalhabib99](https://github.com/vishalhabib99)
